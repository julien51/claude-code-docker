# ABOUTME: Build-only compose file. Use `docker compose build claude-code` to build the image.
# ABOUTME: Runtime sessions are launched by claude-start and claude-task, not compose up.
services:
  claude-code:
    build: .
